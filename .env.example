# Environment variables for AURA.ai Chatbot
# Copy this file to .env and update with your actual values

# Base URLs must NOT include request paths — the server appends /v1/chat/completions and /v1/embeddings
LLM_URL=https://your-llm-endpoint.example.com/
EMBEDDING_URL=https://your-embedding-endpoint.example.com/

# Qdrant Vector Database Configuration
# For Qdrant Cloud: https://your-cluster-id.region.aws.cloud.qdrant.io
# For local Qdrant: http://localhost:6333
QDRANT_URL=https://your-cluster-id.region.aws.cloud.qdrant.io
QDRANT_API_KEY=your_qdrant_api_key_here

# Server port (used by docker-compose mapping)
PORT=3000

# Enable local dev mock mode by default? Set to true or false. You can also toggle at runtime via /api/admin/dev-mock
DEV_MOCK=false
